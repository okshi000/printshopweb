# نظام التقارير الشامل - PrintShop

## نظرة عامة

تم إنشاء نظام تقارير شامل ومتقدم يحتوي على تقارير تفصيلية لجميع جوانب النشاط التجاري مع رسوم بيانية تفاعلية متعددة.

## المميزات الرئيسية

### 1. التقارير المالية 💰
- **الملخص المالي**: إجمالي الإيرادات، المصروفات، صافي الربح، والرصيد النقدي
- **الإيرادات حسب الفترة**: تتبع الإيرادات والمصروفات شهرياً
- **تفصيل المصروفات**: توزيع المصروفات حسب الفئات مع نسب مئوية
- **قائمة الدخل**: تقرير الأرباح والخسائر الكامل مع هامش الربح

### 2. تقارير المبيعات 🛒
- **ملخص المبيعات**: إجمالي المبيعات، عدد الفواتير، متوسط القيمة
- **المبيعات حسب المنتج**: تحليل مبيعات كل منتج مع الكميات والإيرادات
- **المبيعات حسب العميل**: تتبع مشتريات العملاء وأنماط الشراء
- **أفضل المنتجات**: قائمة بأكثر المنتجات مبيعاً

### 3. تقارير المخزون 📦
- **ملخص المخزون**: إجمالي الأصناف، القيمة، المخزون المنخفض
- **تفاصيل المخزون**: معلومات كاملة عن كل صنف مع حالة المخزون
- **حركات المخزون**: تتبع جميع الإضافات والسحوبات
- **تقييم المخزون**: توزيع قيمة المخزون حسب الفئات

### 4. تقارير العملاء 👥
- ملخص نشاط العملاء
- تحليل أفضل العملاء
- تقسيم العملاء حسب الفئات
- تتبع نشاط العملاء

### 5. تقارير الديون 💳
- ملخص الديون المستحقة
- الديون حسب العميل
- تقرير أعمار الديون
- تتبع التحصيلات

### 6. تقارير التدفق النقدي 💸
- ملخص التدفقات النقدية
- اتجاهات التدفق النقدي
- التدفقات حسب الفئة
- التوقعات المالية

### 7. تقارير الموردين 🚚
- ملخص الموردين والمشتريات
- تحليل أداء الموردين
- تتبع المدفوعات

### 8. تقارير الأداء 📊
- مؤشرات الأداء الرئيسية (KPIs)
- المقارنات الزمنية
- تحليل النمو
- مؤشرات الكفاءة

## أنواع الرسوم البيانية

### 1. Line Chart (المخطط الخطي)
- مثالي لعرض الاتجاهات عبر الزمن
- يستخدم في: تتبع الإيرادات، المبيعات، التدفق النقدي

### 2. Bar Chart (المخطط الشريطي)
- رائع لمقارنة القيم المختلفة
- يستخدم في: مقارنة المنتجات، العملاء، المصروفات

### 3. Pie Chart (المخطط الدائري)
- مثالي لعرض النسب والتوزيعات
- يستخدم في: توزيع المصروفات، حصة العملاء، تقسيم المخزون

### 4. Area Chart (المخطط المساحي)
- ممتاز لعرض البيانات التراكمية
- يستخدم في: تراكم الإيرادات، التدفق النقدي، الأرباح

## البنية التقنية

### Backend (Laravel)

#### API Endpoints
جميع endpoints تحت المسار: `/api/reports-v2/`

**التقارير المالية:**
- `GET /financial/summary` - الملخص المالي
- `GET /financial/revenue-by-period` - الإيرادات حسب الفترة
- `GET /financial/expense-breakdown` - تفصيل المصروفات
- `GET /financial/profit-loss` - الأرباح والخسائر

**تقارير المبيعات:**
- `GET /sales/summary` - ملخص المبيعات
- `GET /sales/by-customer` - المبيعات حسب العميل
- `GET /sales/by-product` - المبيعات حسب المنتج
- `GET /sales/top-products` - أفضل المنتجات

**تقارير المخزون:**
- `GET /inventory/summary` - ملخص المخزون
- `GET /inventory/details` - تفاصيل المخزون
- `GET /inventory/movements` - حركات المخزون
- `GET /inventory/valuation` - تقييم المخزون

**تقارير أخرى:**
- `GET /suppliers/summary` - تقرير الموردين
- `GET /customers/summary` - تقرير العملاء
- `GET /debts/summary` - ملخص الديون
- `GET /debts/by-customer` - الديون حسب العميل
- `GET /cash-flow/summary` - التدفق النقدي
- `GET /cash-flow/trend` - اتجاه التدفق النقدي
- `GET /dashboard/metrics` - مقاييس لوحة التحكم

#### Controllers
- `ReportsController.php` - يحتوي على جميع دوال التقارير

### Frontend (React + TypeScript)

#### الملفات الرئيسية

**Types:**
- `src/types/reports.ts` - جميع أنواع TypeScript للتقارير

**API Services:**
- `src/api/reports.ts` - دوال API للتعامل مع التقارير

**المكونات:**
- `src/components/charts/` - مكونات الرسوم البيانية
  - `AreaChartComponent.tsx`
  - `BarChartComponent.tsx`
  - `LineChartComponent.tsx`
  - `PieChartComponent.tsx`
- `src/components/reports/DateRangeFilter.tsx` - فلتر التاريخ

**الصفحات:**
- `src/pages/reports/ReportsOverview.tsx` - الصفحة الرئيسية للتقارير
- `src/pages/reports/FinancialReports.tsx` - التقارير المالية
- `src/pages/reports/SalesReports.tsx` - تقارير المبيعات
- `src/pages/reports/InventoryReports.tsx` - تقارير المخزون

#### Routes
```typescript
/reports - الصفحة الرئيسية للتقارير
/reports/financial - التقارير المالية
/reports/sales - تقارير المبيعات
/reports/inventory - تقارير المخزون
```

## الفلاتر المتاحة

جميع التقارير تدعم الفلاتر التالية:
- `start_date` - تاريخ البداية
- `end_date` - تاريخ النهاية
- `period` - الفترة (daily, weekly, monthly, quarterly, yearly)
- `customer_id` - معرف العميل
- `supplier_id` - معرف المورد
- `product_id` - معرف المنتج
- `category_id` - معرف الفئة
- `status` - الحالة

## كيفية الاستخدام

### 1. عرض التقارير
```typescript
// استخدام مكون التقرير
import { FinancialReports } from '@/pages/reports';

function MyComponent() {
  return <FinancialReports />;
}
```

### 2. استدعاء API
```typescript
import { reportsApi } from '@/api/reports';

// جلب الملخص المالي
const summary = await reportsApi.financial.getSummary({
  start_date: '2024-01-01',
  end_date: '2024-12-31'
});

// جلب المبيعات حسب المنتج
const salesByProduct = await reportsApi.sales.getByProduct({
  start_date: '2024-01-01',
  end_date: '2024-12-31'
});
```

### 3. استخدام الرسوم البيانية
```typescript
import { LineChartComponent } from '@/components/charts';

<LineChartComponent
  data={chartData}
  xKey="date"
  yKeys={[
    { key: 'revenue', name: 'الإيرادات', color: '#10b981' },
    { key: 'expenses', name: 'المصروفات', color: '#ef4444' }
  ]}
  height={400}
  formatValue={(value) => `$${value.toLocaleString()}`}
/>
```

## المكتبات المستخدمة

- **Recharts**: للرسوم البيانية التفاعلية
- **React Query**: لإدارة البيانات والـ caching
- **date-fns**: لمعالجة التواريخ
- **Radix UI**: لمكونات الواجهة
- **Tailwind CSS**: للتنسيق

## التحسينات المستقبلية

- [ ] تصدير التقارير إلى PDF
- [ ] تصدير التقارير إلى Excel
- [ ] جدولة التقارير التلقائية
- [ ] إرسال التقارير عبر البريد الإلكتروني
- [ ] تقارير مخصصة قابلة للحفظ
- [ ] مقارنة الفترات الزمنية
- [ ] التوقعات والتحليلات المتقدمة
- [ ] تقارير الموردين التفصيلية
- [ ] تقارير العملاء المتقدمة
- [ ] مؤشرات أداء إضافية

## الملاحظات الهامة

1. جميع التقارير تستخدم React Query للـ caching والأداء الأفضل
2. الرسوم البيانية مصممة لتكون responsive وتعمل على جميع الأجهزة
3. يمكن تخصيص الألوان والتنسيقات بسهولة
4. جميع النصوص بالعربية مع دعم RTL
5. البيانات يتم تنسيقها بالعملة المصرية (EGP)

## الدعم والمساعدة

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق التطوير.

---

تم التطوير بواسطة فريق PrintShop © 2024
