# بيانات الاتصال بقاعدة البيانات (Container-based MySQL) 🛢️

تم إنشاء قاعدة البيانات بنجاح باستخدام **Azure Container Instances**.
هذه البيانات هي التي سنستخدمها في إعدادات Laravel لاحقاً.

---

### 🔑 بيانات السيرفر (Server Details):

| المفتاح | القيمة | ملاحظات |
| :--- | :--- | :--- |
| **Host (DNS Name)** | `mydb-norway-5632.norwayeast.azurecontainer.io` | هذا هو الرابط الدائم للسيرفر |
| **IP Address** | `4.220.9.209` | بديل للـ Host في حال واجهت مشاكل DNS |
| **Port** | `3306` | المنفذ الافتراضي |
| **Region** | `Norway East` | المنطقة التي يعمل فيها السيرفر |

---

### 🔐 بيانات الدخول (Credentials):

| المفتاح | القيمة |
| :--- | :--- |
| **Username** | `root` |
| **Password** | `Abdo_Student123!` |

---

### 🛠️ أوامر هامة:

#### 1. للاتصال بالسيرفر وإنشاء قاعدة البيانات (نسخ/لصق في التيرمينال):
```bash
mysql -h mydb-norway-5632.norwayeast.azurecontainer.io -u root -p
# سيطلب منك الباسورد، اكتب: Abdo_Student123!
```

#### 2. أمر SQL لإنشاء قاعدة البيانات (بعد الدخول):
```sql
CREATE DATABASE printshop_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;
```

---

### ⚠️ تنبيه هام جداً:
بما أن هذا **Container** وليس **Managed Database**:
1. **البيانات متطايرة (Volatile):** إذا قمت بحذف الكونتينر `mysql-container-final`، ستضيع جميع البيانات داخله إلا إذا كنت قد ربطته بـ Volume خارجي (Azure File Share).
2. **النسخ الاحتياطي:** يجب عليك عمل نسخ احتياطي يدوي (Dump) كل فترة.

---
📅 *تاريخ الإنشاء: 30 يناير 2026*
